import { createStyles } from "@material-ui/core";

const styles = createStyles({
  root: {
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
    padding: 16,
    margin: 8,
  },
  button: {
    marginRight: 16,
  },
});

export default styles;
